<p id="notice"><%= notice %></p>

<h1 style="text-align: center;">Bienvenue sur mon site</h1>

    <div class="row">
    <% @posts.each do |post| %>
    <div class="col-md-3" style="text-align: center; height: 10%;background-color: light-grey;">
    <%= post.name %>
    <%= link_to 'Ajouter un like', liike_path(user_id: current_user.id),remote: true%>
        <%= link_to 'Plus dinformation', post %>
        <%= link_to 'Edit', edit_post_path(post)%>
        <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }%>
              </div>
      <% post.likes.each do |product| %>
     <div class="col-md-2">
      </div>
      <div class="col-md-2">
     <%= like.all.count %>
     </div>
      <% end %>
    <% end %>
</div>

<%= link_to 'New Post', new_post_path %>
